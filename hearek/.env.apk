# APK build uchun env. `npm run build:apk` shu fayl bilan vite ishga tushiriladi.
# WebView capacitor://localhost dan boshlanadi — relative `/api` ishlamaydi,
# shu sababli to'liq publik URL belgilanadi.
VITE_API_BASE_URL=https://hearak.yuksalish.dev
